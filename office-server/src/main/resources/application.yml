server:
  port: ${SERVER_PORT:4100}

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 11MB

app:
  upload:
    dir: ${UPLOAD_DIR:./uploads}

logging:
  level:
    com.producttracker.officeserver: INFO
